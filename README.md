# Ecclesia

Modern church directory and certificate management scaffold.

## Structure

- `backend/` Django + DRF + SimpleJWT + PostgreSQL
- `frontend/` React + Vite + Tailwind CSS + i18next

## Included

- Custom user model with household fields
- Member directory API with fuzzy search
- Certificate request workflow with PDF generation helper
- Accessible React directory cards and dashboard shell
